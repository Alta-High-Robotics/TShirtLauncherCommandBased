package frc.robot;

/**
 * RobotData is used for data for subsystems including (but not limited to) motor speed.
 * */
public class RobotData {
	public static final double
		//negative speed will cause the motor to flip directions (speed is absolute |speed|)
		LAUNCHER_RAISE_SPEED = 0.2,
		LAUNCHER_LOWER_SPEED = 0.2;
}